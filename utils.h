#ifndef UTILS_H
#define UTILS_H
#pragma once
#include "common.h"
using namespace std;
//utils
extern bool isNumber(string line);
extern int binary_to_dec(string input);
extern string dec_to_binary(int number);

#endif //UTILS_H